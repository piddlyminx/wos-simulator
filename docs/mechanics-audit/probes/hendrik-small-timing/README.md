# Hendrik small timing discriminator

The preceding full-kit3/3/3 Marksman capture returned471 defenders against the current476; starting S3 on round2 instead of3 reproduced471 retrospectively. This follow-up was frozen before its battle to distinguish that phase hypothesis at a smaller scale. It preserves the full kit, exact T6 troop keys, source-count ceiling and fractional survivor state.

WIP attacks with250 T6 Marksmen and Hendrik3/3/3; minxxx defends without heroes using150 Infantry,60 Lancers and60 Marksmen. Stats are conditional on the preceding verified report and must be checked in the new report. No generation bonus is added.

| Frozen candidate | Defending I/L/M; total | Displayed-stat sensitivity total |
| --- | --- | --- |
| Current |19/34/25;78|78|
| S3 first2, every3 |16/32/23;71|70–71|
| S2 first1, every4 |16/34/25;75|75–76|
| S2 delay1 |20/34/25;79|79|

All four candidates preserve all defender lines. The563 shared vectors include nominal stats, two opposing corners, individual ±0.05 coordinate perturbations and512 interior vectors. This is a precision sensitivity screen, not an exhaustive bound. The seven-survivor primary separation is useful at250 versus270 troops; the closer S2 alternatives may remain indistinguishable within the usual1–2 survivor aim.

`manifest.json` guards the engine archive, all29 runtime source hashes, complete candidate configs, inputs, protocol, spec and scripts. The reused archive was verified against every current source file before freezing. `prediction.json` and `capture-decision.json` preserve the pre-capture forecast and hash. `predict.mts NEW_OUTPUT [FRESH_INPUT]` replays the frozen models to a new path outside the package; the optional input supplies fresh report stats. Every replay retains both armies' endpoints and uses attacker-minus-defender score, including draws. The full kit and actual troop keys must additionally be checked against the report before interpretation.

## Captured outcome

The report at **2026-09-07 07:31:27**, X787/Y577, records **68 defending survivors:16 Infantry,31 Lancers and21 Marksmen**, with zero attackers. All24 displayed stats, exact troop counts and the unchanged full Hendrik3/3/3 kit match the frozen input. [Observation metadata](observation.json) preserves the checked images and casualty totals. The attacker and defender mail IDs are2734692464124634 and2734692464124635; these two reports describe one battle.

The report shows S1 once, S2 eight times, and S3 eleven times with27 displayed kills. No game round count is shown. The frozen current prediction misses by10 overall and3/3/4 per line. S3-first2 misses by3 overall and0/1/2 per line; its displayed-stat screen reaches70, two above the game68. This is evidence favoring earlier S3 timing while retaining a small residual and uncertainty about exact activation reporting. It does not justify inferring game rounds or fitting coefficients.

The preceding prospective protocol and all forecast hashes remain unchanged. `captured-prediction.json` is a separate fresh-input replay. The [retrospective counter-offset comparison](retrospective-counter-offset/) considers the earlier two captures together with this result; its interpretation was developed after outcome exposure.
