# Gwen single-target design — not captured

The bounded screen did not provide a strong enough endpoint discriminator to justify a new battle. No emulator action, game capture or fixture was created for these formations.

The [protocol](protocol.json) froze five named models before simulation: current S3 first5/every4, first5/every5, first4/every5, first6/every4 and S3 omitted. All retain the full available Gwen3/1/1 kit and the exact previously verified report stats, T6 Marksmen versus T6 Infantry. All earlier Gwen outcomes were known; no outcome for this new formation screen was collected or read.

The20 formations use150/250/350/450/550 Marksmen against300/500/700/900 Infantry. [screen.json](screen.json) preserves all100 deterministic predictions. Current versus first5/every5 differs by at most four survivors; first4/every5 and first5/every5 differ by at most one. Every5 is also within one survivor of the omission model throughout this screen. Near the most sensitive selected point,350 versus900, the five predictions are respectively attacker22,18,17,12 and19.

One enemy line removes sibling fanout and backline-depletion feedback, but it retains the uncertain S1/S2 charge lifecycle. Additional S3 damage can consume a modifier that would otherwise affect a later attack. Thus the near-equivalence of an omission is a conditional simulator finding, not evidence that S3 is ineffective in game. Activation-count ratios could still constrain timing, but the three existing mixed-target reports already provide that kind of evidence. This screen does not justify another battle solely to obtain a similarly conditional ratio.

The source input, full config, runtime archive and source hashes are preserved. No report-precision screen or capture specification was produced because the nominal screen already failed the design objective. The unresolved mixed-target disagreement remains unchanged; no production fix follows.
