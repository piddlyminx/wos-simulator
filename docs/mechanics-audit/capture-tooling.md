# Capture tooling changes during the evidence audit

The two early Gwen follow-up attempts failed during hero selection, before an attacker was dispatched. Their logs remain under `tmp/mechanics-audit-2026-09-07/gwen/`. A shorter manual scroll exposed the existing Gwen template. The automatic picker scroll was reduced from240 to120 pixels to retain row overlap despite momentum; its existing13 assignment tests passed. During the later550-versus900 capture, Gwen was found and assigned after the first short swipe (06:28:44–06:28:50 in `tmp/mechanics-audit-2026-09-07/gwen-small-timing-550-capture.log`). This is a successful live use of the adjusted scroll, not proof that every earlier miss had the same cause.

Sonya lacked a hero-card template, so the first attempt stopped before deployment. `skill/templates/heroes/Sonya.png` is the exact90×40 card crop from [the captured picker](probes/sonya-buffs-cadence/hero-picker.png). The same card was opened to verify [Sonya's identity](probes/sonya-buffs-cadence/hero-identity.png) and [full1/1/0 skill kit](probes/sonya-buffs-cadence/hero-skills.png). The retry completed and produced the accepted1175-survivor battle. No failed selector attempt is counted as a game observation.

`capture-hero-skill-details HERO --output-dir DIR` captures the persistent skill details pane in the bottom third of the hero's Skills screen, including Expedition descriptions and visible upgrade previews through `wosctl`. It preserves the full roster cache, records the current levels, and selects only the skill icons. Twenty-four focused offline tests cover allowed taps, changed identity/levels, partial captures, instance locking and cache preservation. The first [live Gwen capture](hero-tooltips/minxxx-gwen-2026-09-07/README.md) completed with all three distinct skill descriptions and the unchanged3/1/1 kit; root visually checked the images against the definition. Displayed coefficients supplement battle evidence and do not establish unshown timing or damage semantics.

New captures write `skill-details.json` with capture kind `hero_expedition_skill_details`. Earlier capture bundles retain their original `hero-tooltips` paths and `tooltips.json` filenames so existing evidence references and recorded hashes remain valid.
