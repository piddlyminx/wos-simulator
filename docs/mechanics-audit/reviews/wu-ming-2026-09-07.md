# Wu Ming evidence review — 2026-09-07

**Adoption update:** the core arithmetic correction now reproduces the archived 200-survivor battle exactly. A [fresh production replay](outer-army-ceil/adoption-check.json) also matches all eight active fixtures below exactly. See the [adoption review](outer-army-ceil/adoption.md). The original numerical review below is preserved: “current” in its table and counterfactuals means the runtime before that correction, identified by the original replay hashes.

The accepted in-game fixtures support specific level-3 Wu Ming behavior. They do not establish every branch of his skills. At the original review, eight active deterministic fixtures agreed within one survivor; an earlier capture, replaced in the active corpus by a newer battle, still disagreed by 19 survivors.

This is a retrospective review: all outcomes were already exposed. All recorded game outcomes are accepted in-game evidence unless explicitly invalid or obsolete, without requiring a report image. Counterfactuals below are simulator diagnostics; no skill was disabled in a live battle. [Original replay JSON](wu-ming-2026-09-07.json) preserves exact inputs, observations, source hashes, applied-effect shapes and pre-correction counterfactuals. Its script refuses to overwrite that snapshot with changed source hashes; use the separate adoption replay for the corrected runtime.

All Wu kits below retain 3/3/3. Positive scores mean attacker survivors; negative scores mean defender survivors. Report stats are used unchanged, without adding hero-generation stats or searching for a fit.

| Accepted fixture | Game | Current | Discriminating current counterfactual |
| --- | ---: | ---: | --- |
| `wu_ming_s1_isolation_nc`: 500 T6 Infantry vs 710 Marksmen | +178 | +178 | Omit S1: +105; omit S2: +121; S1 normal value 18 instead of 15: +189 |
| `wu_ming_s2_isolation_nc`: 500 T6 Lancers vs 340 Marksmen | +40 | +39 | Omit S2: −61; broaden S1 to all troops: +137; apply S3 to normal damage: +136 |
| `wu_ming_marksman_vs_infantry_nc`: 500 T6 Marksmen vs 350 Infantry | −11 | −11 | Amplify Ranged Strike's increment by S3's 15%: +17; amplify whole normal job: +123 |
| `wu_ming_bradley_current_nc`: Wu + Bradley 4/4/4, 170 each T6 vs 900 Infantry + 900 Lancers | +112 | +112 | Move S2 to a separate normal-damage bucket: +114; S3 on normal jobs: +279 |
| `wu_ming_solo_current_nc`: 170 each T6 vs 380 Infantry + 380 Lancers, newer stats | −223 | −223 | Combined-kit agreement only |
| `wu_ming_rounding_followup_nc` | −180 | −179 | Combined-kit agreement only |
| `renee_wu_ming_s1_damage_kind_nc`: Renee 4/4/4 vs defending Wu | +359 | +360 | Corroborates the existing normal-job model; no new counterfactual here |
| `retarget_lancer_screen_nc`, no heroes | −303 | −303 | Historical documented attack-time retargeting alternative: −282; not rerun here |

The S1 fixture's original precommit incorrectly capped two losing candidates at 100 rounds. Its description explicitly preserves that mistake and the later 1500-round correction. The +105/+121 alternatives are retrospective corrections, not accurate original forecasts. The S2 fixture records its +39/−61/+137/+136 interpretations as frozen before capture.

**Supported dimensions and limits.** S1's normal branch protects Infantry at a value consistent with the configured level-3 value 15; the Lancer discriminator rejects broadening it to every troop. Current arithmetic divides matching damage by `1 + 0.15`, rather than multiplying by `0.85`. S2 contributes to normal attacks from Infantry, Lancers and Marksmen, with the dedicated Lancer fixture isolating its level-3 contribution. These do not identify cross-level scaling, exact stacking with another protection modifier, or S1's positive skill-damage branch.

S3's exclusion of normal jobs, including the Ranged Strike increment, has discriminating negative evidence. Every current trace in this review has zero skill-kind jobs, and none applies `ElementalResonance/1` or `ShadowsEvasion/2`. Consequently neither S3's positive amplification nor S1's skill-damage reduction is validated here. Other Wu-containing fixtures remain available for a separate causal review; mere hydration cannot fill those gaps. No reviewed case exercises Iron Bastion, a widget, joiners or engagement-mode gates.

The older [mixed-target investigation](../../mixed-target-parity-investigation-2026-08.md) overstates the Bradley result as proof of S2's shared additive bucket. With exact current fixture inputs, separate-bucket +114 is only two survivors from +112, inside the user's usual deterministic tolerance. Retain `active.hero.damage.up` as the existing model, but leave its grouping unresolved. An aggregate endpoint alone cannot resolve cancelling per-source errors.

**Historical contradiction, now resolved by the core correction.** Commit `87cbbeac885b977983369d8882e31ec0b1bbd0b3` replaced the earlier Wu solo inputs and game outcome with a new capture. The [archived earlier fixture](wu-ming-solo-superseded.json), copied exactly from that commit's parent, records −200. At the original review, runtime on those exact older inputs still gave −219; the newer −223 agreement did not explain or supersede that earlier result. Removing the outer army-term ceiling now gives −200 without changing Wu, Ranged Strike, source-count ceiling, fractional survivor state, or retargeting. The archive and original −219 replay remain preserved. Its absence from the active inventory remains a coverage limitation, but the 19-survivor parity residual is closed. The old hidden-precision and per-source-kill investigations remain historical diagnostics; their full sensitivity searches were not rerun here.

Runtime now uses explicit `applies_to_damage_kinds` to select the S1/S3 branches, denominator buckets for S1 and numerator buckets for S2/S3. The loop is troop-major, as the [attack-trigger review](../../attack-trigger-regime-2026-08.md) records; the older mixed-target note's side-major description is stale. That structural choice, blocked/exhausted cadence, and generic lifecycle tests are not independently validated game behavior by this review.
